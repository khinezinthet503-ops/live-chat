import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";
import { getDatabase } from "firebase/database";

// Your Web app's Firebase configuration
// (You can copy this directly from your Firebase Console Project Settings)
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "vue-blog-system-5bb62.firebaseapp.com",
  projectId: "vue-blog-system-5bb62",
  storageBucket: "vue-blog-system-5bb62.firebasestorage.app",
  messagingSenderId: "353049653856",
  appId: "YOUR_APP_ID",
  // Optional: databaseURL is required if you are using Realtime Database
  databaseURL: "https://test-db.firebaseio.com" 
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

// Connect and export your database reference
export const db = getFirestore(app); // For Cloud Firestore
// export const rtDb = getDatabase(app); // For Realtime Database
